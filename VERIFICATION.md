# 重新打磨驗證紀錄 · 2026-09-18

## 資料

`node tests/namepool.cjs` 通過：480 字、零重複、必填欄位齊全。
五行數量：金 83、木 104、水 101、火 95、土 97。
12,000 次取名涵蓋自動／五行、一字／二字，另測無法滿足筆畫條件時的 fallback、
收集字優先與喜用神優先。沒有空名、重複字或不符指定五行的結果。

## 功能（本機真瀏覽器）

Chromium 與 WebKit 測試通過：

- 無啟動錯誤、canvas 出現、無外部執行期請求。
- 十下點擊新增一個名字，揭曉中再十下仍新增，4.6 秒後自動收合。
- 線段滑動掃破泡泡、收字、增加能量；五種喜用神實際操作均產生對應名字。
- 互動前未建立 AudioContext；互動後 running 且輸出波形非零。
- 兩音軌獨立靜音、輸出歸零、重載記住、單獨恢復音效時音樂仍靜音。
- 舊版名字資料相容、單筆刪除、清空、非陣列損壞資料安全回復。
- Chromium 全螢幕進入／退出；兩引擎均驗證無 API 的提示 fallback。
- 390×844、820×1180、1180×820、844×390 控制項沒有超出畫面。
- 150 次密集點擊後粒子 ≤700、音訊聲部 ≤48、貼圖未超過有限字庫上限。

## 效能實測與限制

- 可見 Chromium、Apple M3／ANGLE Metal：10 秒平均 60.07 fps，95 百分位 18.4ms。
- WebKit 桌面：10 秒平均 60.02 fps，95 百分位 17ms。
- WebKit 模擬 iPad 820×1180、DPR 2（實際渲染上限 1.5）：120 秒持續觸控，
  469 下、51 個名字、119 次破泡；平均 60.00 fps，95 百分位 19ms，零錯誤。
  結束時 229 張貼圖、54 顆活動粒子，未持續無限增長。
- 無頭 Chromium 的第一次量測僅 34.14 fps；不能以它代表實機 GPU 效能。

以上為 Mac 上的瀏覽器與模擬尺寸，不是實體 iPad 的持續 60fps 保證。
聲音以 WebAudio 波形及增益驗證，沒有以實體 iPad 喇叭進行主觀聽感測試。
繁體筆畫／拼音核對 Unihan；構形與五行是參考分類，並非字源學專家逐字審訂。

## 重跑

依 README 安裝測試專用 Playwright，啟動 `python3 -m http.server 8123`。
執行 `tests/namepool.cjs`、`tests/browser.cjs`、`tests/soak.cjs`。
瀏覽器測試可把正式網站網址作為參數；`QA_HEADED=1` 使用可見 Chromium。
測試截圖與 JSON 輸出預設放 `/tmp/wubaobao-qa`，不加入網站素材。
